# 📘 Векторная база данных с использованием LangChain, Chroma и OpenAI Embeddings

Этот проект выполнен в рамках обучающего курса и демонстрирует базовые навыки работы с векторными базами данных, эмбеддингами и текстовой предобработкой для последующего построения RAG-моделей.

Основная цель — **создать собственную векторную базу**, преобразовать текст в чанки, построить эмбеддинги и сохранить их в ChromaDB.

---

## 🚀 Функциональность проекта

В проекте реализовано:

### ✔ Загрузка исходного текста  
Файл `main_info.md` используется как источник данных.

### ✔ Разбиение текста на чанки  
С помощью `RecursiveCharacterTextSplitter` текст разбивается на части размером ~1000 символов.

### ✔ Генерация векторных представлений  
Используются эмбеддинги `OpenAIEmbeddings` из LangChain.

### ✔ Создание и сохранение векторной базы  
Данные сохраняются в локальную ChromaDB.

### ✔ Проверка успешного создания коллекции  

```python
vectordb._collection.count()
Результат:

12

Это означает, что все чанки успешно обработаны и сохранены.

---

## 🛠 Используемые технологии

| Технология | Описание |
|-----------|----------|
| **Python** | Основной язык разработки |
| **Google Colab** | Среда выполнения ноутбука |
| **LangChain** | Работа с текстом, эмбеддингами и векторными хранилищами |
| **OpenAI Embeddings** | Создание числовых векторных представлений текста |
| **ChromaDB** | Локальное векторное хранилище |
| **Markdown / Jupyter Notebook** | Документация и ноутбуки |

---

## 📂 Структура проекта

```bash
.
├── vectorbaza.ipynb       # Основной ноутбук с кодом
├── main_info.md           # Текст, который преобразуется в векторную базу
└── screenshots/           # Скриншоты выполнения (опционально)

🔐 Безопасная работа с OpenAI API Key

В проекте используется безопасный способ ввода API-ключа:
import os
from getpass import getpass

os.environ["OPENAI_API_KEY"] = getpass("Enter your OpenAI API key: ")
✔ Ключ не хранится в ноутбуке
✔ GitHub не обнаруживает секретов
✔ Полностью безопасно при публикации
▶ Как запустить проект
1️⃣ Установить зависимости
pip install langchain chromadb openai tiktoken
2️⃣ Запустить ноутбук

Открыть vectorbaza.ipynb в Google Colab или Jupyter Notebook.

3️⃣ Ввести OpenAI API key вручную
4️⃣ Выполнить все ячейки

После создания базы должно появиться:
12
📎 Пример кода создания векторной базы

from langchain.text_splitter import RecursiveCharacterTextSplitter
from langchain_openai import OpenAIEmbeddings
from langchain_community.vectorstores import Chroma

splitter = RecursiveCharacterTextSplitter(chunk_size=1000, chunk_overlap=200)
documents = splitter.split_documents(raw_docs)

embeddings = OpenAIEmbeddings()

vectordb = Chroma.from_documents(
    documents=documents,
    embedding=embeddings,
    persist_directory="."
)

🎯 Результат

В рамках задания была выполнена:

предобработка текста

генерация эмбеддингов

создание локальной ChromaDB

проверка количества сохранённых чанков

подготовка ноутбука и отчёта

Проект может быть основой для будущих RAG-приложений.

💬 Контакты

Автор: **Ekaterina Luoto**

Проект создан в рамках обучения работе с LangChain и векторными базами.

vectordb.persist()
